
function Test(){
    const expression="Rahul Yadav";
    //all the logical code will be written here
    return(
        // Everything written inside the {} is Javascript evaluated expression
        //{} inside curly braces we can't write like if else or whole logic , we have privilege to write only evaluated output
      <h1>Hello {expression}</h1>
    )
}

export default Test;